# https://docs.amvera.ru/ — окружение Docker, порт 80, диск /data для SQLite
meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  containerPort: 80
  persistenceMount: /data

"""Текст и обложка стартовой новости Volnovoi Cult."""

from __future__ import annotations

import shutil
from pathlib import Path

LAUNCH_NEWS_TITLE = "Volnovoi Cult — то, чего ещё не было на рынке"

LAUNCH_NEWS_BODY = """Раньше вы смотрели на чужие сделки вслепую: красивые скрины, громкие обещания, тишина, когда цена пошла не туда.

Volnovoi Cult — другая история. Это не «канал с сигналами». Это витрина живого рынка: кто торгует, как держит дисциплину, что открыто сейчас, что уже закрыто с результатом. Всё на виду — до того, как вы решите платить.

Что изменилось для вас

Лента стала честной
• Сразу видно: что в игре прямо сейчас и что уже отработано
• Активные сделки — сверху, как на радаре
• Закрытые — отдельно, без путаницы
• Вход по лимитке — вы получаете сигнал, когда сделка реально в рынке

ТОП — не красивые аватарки, а отбор
• Трейдеры Cult — те, кто прошёл через результат
• Ранги, статистика, динамика — не слова, а цифры
• Кандидаты — отдельная лига: доказывают себя в своей карточке, не засоряя общую ленту
• Хотите в игру — открываете карточку, видите сделки, рост, провалы и победы как в мини-бирже таланта

Кандидат = почти топ-трейдер, но со своей сценой
• Та же серьёзность: ранги, правила, дисциплина
• Свои сделки — в карточке, как личная витрина
• Можно закрыть позицию по рынку, когда рынок требует решения
• Чужие карточки компактные — ваша всегда развёрнута и на первом месте

Почему это уникально

На рынке полно мест, где продают надежду.

Здесь показывают процесс:
• кто в рынке,
• кто ждёт входа,
• кто уже закрылся в плюс или минус,
• кто растёт в рейтинге,
• кто только заходит в отбор.

Это ближе к marketplace трейдеров, чем к закрытому чату.
Не «поверь нам». А «смотри сам — и решай».

Такого формата в массовом доступе ещё не было.
Мы его собрали. И докручиваем каждый день.

Сейчас — лучший момент зайти

30 дней всё бесплатно — тестовый режим.

Без отговорок:
• лента целиком,
• активные сигналы,
• уведомления,
• путь кандидата в CULT.

Потрогайте продукт руками. Поймите, зачем он вам.
После теста — только платный доступ. Честно и прозрачно, как весь Cult.

Кому это
• Новичку — чтобы не учиться на чужих обещаниях, а смотреть на реальные сделки
• Опытному — чтобы видеть, кто реально держит уровень
• Амбициозному — чтобы выйти в кандидаты и заявить о себе на сцене, а не в шуме чата

Volnovoi Cult — marketplace, где рынок говорит сам за себя.

Заходите. Смотрите. Сравнивайте.
И если чувствуете — входите в игру.

Это только начало."""

PENDING_NEWS_NOTIFY_FILE = ".pending_news_notify_id"
SEED_MARKER_V1 = ".seeded_volnovoi_cult_launch_news_v1"
SEED_MARKER_V2 = ".seeded_volnovoi_cult_launch_news_v2"

LAUNCH_NEWS_COVER_FILES = (
    "news-cover-volnovoi-cult-full.png",
    "news-cover-volnovoi-cult.png",
    "news-update-june-2026.png",
)


def launch_news_cover_path() -> Path | None:
    for name in LAUNCH_NEWS_COVER_FILES:
        docker_path = Path("/app/news-assets") / name
        if docker_path.is_file():
            return docker_path
        repo_path = Path(__file__).resolve().parents[2] / "docs/news-assets" / name
        if repo_path.is_file():
            return repo_path
    return None


def copy_news_cover(media_root: Path, post_id: int, image_path: Path) -> str:
    ext = image_path.suffix.lower()
    folder = media_root / "news" / str(post_id)
    folder.mkdir(parents=True, exist_ok=True)
    rel = f"news/{post_id}/cover{ext}"
    shutil.copy2(image_path, media_root / rel)
    return rel

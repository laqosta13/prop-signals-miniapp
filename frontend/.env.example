# Полный URL API в проде (HTTPS). В dev можно не задавать — используется proxy /api → localhost:8000
# VITE_API_URL=https://api.yourdomain.com

# Локально: ваш Telegram user id и в backend .env оставьте BOT_TOKEN пустым + TELEGRAM_ADMIN_IDS=<тот же id>
# VITE_DEV_TELEGRAM_USER_ID=123456789
